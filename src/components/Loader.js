import React from 'react';
import { Audio, Circles, ColorRing, Dna, InfinitySpin, Radio } from 'react-loader-spinner';

export default function Loader() {
    return (
        <div>
            <ColorRing/>
        </div>
    );
}
